version: 2.1
workflows:
  testing:
    jobs:
      - runner-test
jobs:
  runner-test:
    machine: true
    resource_class: circle-suzuki/winserver2019
    steps:
      - run: echo "Hi I'm on Runners!"
